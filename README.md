# Dot Phials

Dot Phials is a personal, reproducible system and dotfiles repository.
It is structured to support multiple operating systems, with the current focus on Arch Linux.

The repository separates:
- system-level dependencies
- user configuration (symlinked)
- service-specific configuration

The repository is designed to be idempotent: running install scripts multiple times is safe.
